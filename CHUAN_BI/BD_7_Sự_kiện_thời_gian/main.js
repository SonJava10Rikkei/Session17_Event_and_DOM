/**
 * setTimeout(function, milliseconds)
 * setInterval(function, milliseconds) //Thực thi một chức năng, sau khi đợi một số mili giây được chỉ định.
 *
 *
 *
 */

function myFunction() {
    alert('Hello');
}

// myVar = setTimeout(function, milliseconds);
// clearTimeout(myVar);

// window.setTimeout(function, milliseconds);

